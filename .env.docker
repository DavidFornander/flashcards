# Docker Environment Variables
# Copy this file to .env and update with your values

# Data Provider Selection
DATA_PROVIDER=firestore

# Backend Port (exposed to host)
PORT=8101

# Frontend API URL (for Docker, uses service name)
# This is set in docker-compose.yml, but you can override here
# VITE_API_URL=http://backend:8101
